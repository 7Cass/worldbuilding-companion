# Next.js for the Full-Stack Local App

The local web app will use Next.js even though SSR and SEO are not product requirements. The value is a single TypeScript application that can host the dashboard UI, server routes/actions, Notion SDK access, Drizzle/Postgres access, and AI provider calls without splitting frontend and backend before the MVP workflow is proven.
