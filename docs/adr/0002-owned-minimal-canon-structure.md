# Owned Minimal Canon Structure

The companion app will start by creating and owning a minimal Notion structure for each Canon instead of trying to infer arbitrary user layouts. This keeps the product focused on fictional worldbuilding and gives analysis, visualization, generation, import, and export features a stable domain model to rely on, at the cost of requiring users to adopt or map into the app's structure.
