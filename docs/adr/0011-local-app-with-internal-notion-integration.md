# Local App with Internal Notion Integration

The MVP will run as a local app for a single creator, using local Postgres and an internal Notion integration rather than public OAuth or hosted SaaS infrastructure. This minimizes authentication, deployment, billing, and tenant isolation work while validating the Notion-first worldbuilding workflow with real data.
