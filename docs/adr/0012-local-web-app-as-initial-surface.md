# Local Web App as the Initial Surface

The MVP will be a local web app opened in the browser rather than a desktop app or CLI/TUI. A browser-based interface is the best fit for the Canon Dashboard, Entity Workspace, Review Queue, graph and timeline views, and future AI actions, while a desktop wrapper can be considered later if the product grows enough to justify packaging.
