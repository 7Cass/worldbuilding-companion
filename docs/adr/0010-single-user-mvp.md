# Single-User MVP

The MVP will target a single creator rather than a multi-user SaaS. This keeps the product focused on proving the Notion-first worldbuilding workflow without billing, tenant management, collaboration, or public onboarding, while still allowing the architecture to introduce accounts and public Notion OAuth later if the product is shared with other creators.
