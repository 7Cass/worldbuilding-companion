# Defer Claims from the MVP

The MVP will not model Claims as first-class Canon elements. Canon elements should still cite Sources, but individual factual assertions will remain embedded in entity fields and descriptions until contradiction analysis needs a more granular model, avoiding a heavy evidence-tracking system before the core worldbuilding workflow is proven.
